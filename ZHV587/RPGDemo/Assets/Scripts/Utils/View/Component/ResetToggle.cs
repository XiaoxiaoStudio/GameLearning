using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using UnityEngine;

namespace Air2000
{
    public class ResetToggle : MonoBehaviour
    {
        public UIToggle StartToggle;
        void OnEnable()
        {
            //UIToggle[] toggles = GetComponentsInChildren<UIToggle>();
            //if (toggles != null && toggles.Length > 0)
            //{
            //    for (int i = 0; i < toggles.Length; i++)
            //    {
            //        UIToggle toggle = toggles[i];
            //        if (toggle)
            //        {
            //            toggle.value = false;
            //        }
            //    }
            //}
            //if (StartToggle)
            //{
            //    StartToggle.togg
            //}
        }
        void Start()
        {
            //UIToggle[] toggles = GetComponentsInChildren<UIToggle>();
            //if (toggles != null && toggles.Length > 0)
            //{
            //    for (int i = 0; i < toggles.Length; i++)
            //    {
            //        UIToggle toggle = toggles[i];
            //        if (toggle)
            //        {
            //            toggle.value = false;
            //        }
            //    }
            //}
            //if (StartToggle)
            //{
            //    StartToggle.value = true;
            //}
        }
    }
}
